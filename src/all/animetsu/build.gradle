ext {
    extName = 'Animetsu'
    extClass = '.Animetsu'
    extVersionCode = 1
    isNSFW = true
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(":lib:playlistutils"))
}
